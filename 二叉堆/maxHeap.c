#include <stdio.h>

// 堆的容量
#define MAX_SIZE 10

// 定义最大堆结构体，这里我们的第一个元素从索引1开始，出于方便
typedef struct maxHeap {
  int size;             // 堆的容量
  int data[MAX_SIZE];   // 堆的底层数组   
} heap;


int main(void) {
  
}
